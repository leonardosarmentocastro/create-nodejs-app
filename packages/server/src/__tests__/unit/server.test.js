const test = require('ava');

// TODO/FFT: maybe use js generators* to mimic "const stub = jest.fn()"? 
test.todo('cors middleware comes plugged in by default');
test.todo('json output prettifier middleware comes plugged in by default');
